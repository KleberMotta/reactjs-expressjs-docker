{
  "name": "backend-com-node",
  "version": "1.0.0",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "ci-install": "rm -rf node_modules && yarn install --frozen-lockfile",
    "dev": "nodemon"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "uuidv4": "^6.2.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "dotenv": "^8.2.0"
  }
}